---
- hosts: localhost
  tasks:
    - name: Install python pip
      apt:
        name: python3-pip
        state: present
    - name: Install docker
      apt:
        name: docker
        state: present
    #- name: start docker
    #  service:
    #    name: docker
    #    state: started
    #    enabled: yes
    - name: Install docker-py python module
      pip:
        name: docker-py
        version: 1.10.6
        state: present
    - name: Start the container
      docker_container:
        name: devapp10
        image: "312314205034/devapp10:{{DOCKER_TAG}}"
        state: started
        published_ports:
          - 0.0.0.0:8064:8080
